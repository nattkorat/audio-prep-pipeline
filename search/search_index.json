{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Audio Prep Pipeline","text":"<p><code>audio-prep</code> converts source audio into pretraining-ready WAV or FLAC files and can optionally split speech into VAD-based chunks. It is designed for dataset preparation before self-supervised speech pretraining workflows such as Wav2Vec2 and XLS-R.</p> <p>Default converted output:</p> Setting Default Format WAV Sample rate 16 kHz Channels 1, mono"},{"location":"#what-it-does","title":"What It Does","text":"<p>The conversion pipeline:</p> <ol> <li>Recursively discovers supported source audio files.</li> <li>Converts audio with FFmpeg.</li> <li>Validates output with <code>soundfile</code>.</li> <li>Writes an optional JSONL manifest.</li> </ol> <p>The chunking pipeline:</p> <ol> <li>Recursively discovers supported source audio files.</li> <li>Decodes audio with FFmpeg.</li> <li>Detects speech with Silero VAD, or an optional energy fallback.</li> <li>Writes speech-only WAV or FLAC chunks.</li> <li>Writes an optional JSONL chunk manifest.</li> </ol>"},{"location":"#why-this-exists","title":"Why This Exists","text":"<p>Large audio corpora often contain corrupt files, inconsistent sample rates, wrong channel layouts, very short clips, and silence-heavy recordings. This project keeps those problems visible by returning per-file results instead of aborting entire batch jobs, and by validating generated files before they are handed to training code.</p>"},{"location":"#quick-example","title":"Quick Example","text":"<p>CLI conversion:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --format wav \\\n    --sample-rate 16000 \\\n    --workers 8 \\\n    --manifest data/manifest.jsonl</code></pre> <p>Python conversion:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, build_manifest, convert_batch, validate_output, write_manifest\n\nconfig = ConversionConfig(output_format=\"wav\", sample_rate=16_000, num_workers=8)\nresults = convert_batch(Path(\"data/raw_mp3\"), Path(\"data/wav16k\"), config)\nvalidations = {\n    result.output: validate_output(result.output, config)\n    for result in results\n    if result.success and result.output is not None\n}\nrecords = build_manifest(results, validations)\nwrite_manifest(records, Path(\"data/manifest.jsonl\"))</code></pre> <p>CLI chunking:</p> <pre><code>audio-prep chunk \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/chunks \\\n    --min-duration-sec 5 \\\n    --max-duration-sec 20 \\\n    --manifest data/chunk_manifest.jsonl</code></pre> <p>Python chunking:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ChunkConfig, build_chunk_manifest, chunk_batch, write_manifest\n\nconfig = ChunkConfig(min_duration_sec=5, max_duration_sec=20)\nresults = chunk_batch(Path(\"data/raw_mp3\"), Path(\"data/chunks\"), config)\nrecords = build_chunk_manifest(results)\nwrite_manifest(records, Path(\"data/chunk_manifest.jsonl\"))</code></pre>"},{"location":"#start-here","title":"Start Here","text":"<ul> <li>Getting Started</li> <li>Installation</li> <li>CLI Reference</li> <li>Pipeline Overview</li> </ul>"},{"location":"#project-links","title":"Project Links","text":"<ul> <li>PyPI package</li> <li>GitHub repository</li> </ul>"},{"location":"architecture/","title":"Architecture","text":"<p>The package is intentionally small and module-oriented.</p> <pre><code>audio_prep/\n\u251c\u2500\u2500 cli.py          # argparse entry point\n\u251c\u2500\u2500 config.py       # conversion configuration\n\u251c\u2500\u2500 converter.py    # discovery and FFmpeg conversion\n\u251c\u2500\u2500 chunker.py      # VAD chunking\n\u251c\u2500\u2500 validator.py    # output validation and duration probing\n\u251c\u2500\u2500 manifest.py     # JSONL manifest records and writer\n\u2514\u2500\u2500 exceptions.py   # package exceptions</code></pre>"},{"location":"architecture/#design-choices","title":"Design Choices","text":""},{"location":"architecture/#ffmpeg-for-decode-and-encode","title":"FFmpeg For Decode And Encode","text":"<p>Source decoding is delegated to FFmpeg because real corpora often contain VBR files, odd headers, unusual containers, corrupt metadata, and other cases that pure Python decoders handle less consistently.</p>"},{"location":"architecture/#result-objects-instead-of-batch-exceptions","title":"Result Objects Instead Of Batch Exceptions","text":"<p>Conversion and chunking return result objects per file. This lets a large corpus finish even when individual files are broken.</p>"},{"location":"architecture/#validation-after-conversion","title":"Validation After Conversion","text":"<p>FFmpeg success is not treated as enough. Converted output is reopened and checked for sample rate, channel count, readability, and minimum duration.</p>"},{"location":"architecture/#chunking-dependencies","title":"Chunking Dependencies","text":"<p>The base package includes Torch, Silero VAD, and tqdm so both <code>convert</code> and <code>chunk</code> are ready after <code>pip install audio-prep-pipeline</code>. Chunker imports are still lazy, so conversion-only commands avoid VAD startup cost.</p>"},{"location":"cli/","title":"CLI Reference","text":"<p>The package installs one command:</p> <pre><code>audio-prep</code></pre> <p>It has two independent subcommands:</p> <ul> <li><code>convert</code>: convert source audio files to WAV or FLAC, validate them, and optionally   write a manifest.</li> <li><code>chunk</code>: split source audio files into speech-only chunks and optionally write a   chunk manifest.</li> </ul>"},{"location":"cli/#audio-prep-convert","title":"<code>audio-prep convert</code>","text":"<pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --format wav \\\n    --sample-rate 16000 \\\n    --channels 1 \\\n    --workers 4 \\\n    --min-duration-sec 0.5 \\\n    --manifest data/manifest.jsonl</code></pre> Flag Default Description <code>--input-dir</code> required Directory scanned recursively for source audio files. <code>--output-dir</code> required Directory where converted output is written. <code>--extensions</code> common FFmpeg audio/video extensions Comma-separated source extensions, or <code>all</code> to pass every regular file to FFmpeg. <code>--format</code> <code>wav</code> Output format, <code>wav</code> or <code>flac</code>. <code>--sample-rate</code> <code>16000</code> Target sample rate in Hz. <code>--channels</code> <code>1</code> Target channel count. <code>--workers</code> <code>4</code> Number of parallel conversion workers. <code>--min-duration-sec</code> <code>0.5</code> Minimum accepted output duration. <code>--overwrite</code> off Rebuild even when an existing output is valid. <code>--normalize-loudness</code> off Apply single-pass EBU R128 loudness normalization. <code>--manifest</code> none Path to write conversion manifest JSONL. <p>Exit codes:</p> Code Meaning <code>0</code> Every discovered file converted and validated. <code>1</code> At least one file failed conversion or validation. <code>2</code> Argument parsing failed."},{"location":"cli/#audio-prep-chunk","title":"<code>audio-prep chunk</code>","text":"<pre><code>audio-prep chunk \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/chunks \\\n    --format wav \\\n    --sample-rate 16000 \\\n    --min-duration-sec 5 \\\n    --max-duration-sec 20 \\\n    --workers 4 \\\n    --manifest data/chunk_manifest.jsonl</code></pre> Flag Default Description <code>--input-dir</code> required Directory scanned recursively for source audio files. <code>--output-dir</code> <code>&lt;input-dir&gt;/chunks</code> Directory where chunks are written. <code>--extensions</code> common FFmpeg audio/video extensions Comma-separated source extensions, or <code>all</code> to pass every regular file to FFmpeg. <code>--format</code> <code>wav</code> Chunk format, <code>wav</code> or <code>flac</code>. <code>--sample-rate</code> <code>16000</code> Resample before VAD and chunk writing. <code>--min-duration-sec</code> <code>5.0</code> Drop chunks shorter than this duration. <code>--max-duration-sec</code> <code>20.0</code> Split longer speech into windows no longer than this. <code>--workers</code> <code>4</code> Number of parallel chunking workers. <code>--overwrite</code> off Rebuild even when an existing chunk is valid. <code>--allow-energy-fallback</code> off Use a lower-quality energy detector if Silero cannot load. <code>--manifest</code> none Path to write chunk manifest JSONL. <p>Exit codes:</p> Code Meaning <code>0</code> Every discovered file produced one or more chunks. <code>1</code> At least one file failed or produced no chunks. <code>2</code> Argument parsing failed."},{"location":"configuration/","title":"Configuration","text":"<p>Configuration is represented by immutable dataclasses in Python and by CLI flags for command-line use.</p>"},{"location":"configuration/#conversion-defaults","title":"Conversion Defaults","text":"<p><code>ConversionConfig</code> controls conversion and validation:</p> Field Default Meaning <code>output_format</code> <code>wav</code> Converted format, <code>wav</code> or <code>flac</code>. <code>sample_rate</code> <code>16000</code> Target sample rate. <code>channels</code> <code>1</code> Target channel count. <code>num_workers</code> <code>4</code> Parallel conversion workers. <code>min_duration_sec</code> <code>0.5</code> Minimum valid output duration. <code>overwrite</code> <code>False</code> Force regeneration of existing outputs. <code>normalize_loudness</code> <code>False</code> Apply FFmpeg <code>loudnorm</code>. <p>CLI:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/flac16k \\\n    --format flac \\\n    --sample-rate 16000 \\\n    --channels 1 \\\n    --workers 8</code></pre> <p>Python:</p> <pre><code>from audio_prep import ConversionConfig\n\nconfig = ConversionConfig(\n    output_format=\"flac\",\n    sample_rate=16_000,\n    channels=1,\n    num_workers=8,\n)</code></pre> <p>Invalid values raise <code>ValueError</code>.</p>"},{"location":"configuration/#chunking-defaults","title":"Chunking Defaults","text":"<p><code>ChunkConfig</code> controls VAD chunking:</p> Field Default Meaning <code>min_duration_sec</code> <code>5.0</code> Drop chunks shorter than this duration. <code>max_duration_sec</code> <code>20.0</code> Split longer speech into windows at this duration. <code>output_format</code> <code>wav</code> Chunk format, <code>wav</code> or <code>flac</code>. <code>sample_rate</code> <code>None</code> Preserve native rate unless a target is supplied. <code>num_workers</code> <code>1</code> Parallel chunking workers for Python API use. <code>overwrite</code> <code>False</code> Force regeneration of valid existing chunks. <code>allow_energy_fallback</code> <code>False</code> Use the energy detector if Silero cannot load. <p>CLI:</p> <pre><code>audio-prep chunk \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/chunks \\\n    --format flac \\\n    --sample-rate 16000 \\\n    --min-duration-sec 5 \\\n    --max-duration-sec 20 \\\n    --workers 4</code></pre> <p>Python:</p> <pre><code>from audio_prep import ChunkConfig\n\nconfig = ChunkConfig(\n    min_duration_sec=5,\n    max_duration_sec=20,\n    output_format=\"flac\",\n    sample_rate=16_000,\n    num_workers=4,\n)</code></pre> <p>The CLI sets <code>--sample-rate 16000</code> for chunking by default. The Python API default is <code>None</code>, which preserves the source sample rate.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide gives the shortest path from raw source audio to a validated dataset manifest.</p>"},{"location":"getting-started/#intended-users","title":"Intended Users","text":"<p>Use this project if you prepare speech or general audio corpora for pretraining, ASR experiments, or downstream dataset curation. It is especially useful when a large corpus may contain bad files and you need a manifest showing exactly what worked and what failed.</p>"},{"location":"getting-started/#expected-input","title":"Expected Input","text":"<p>The CLI scans for common FFmpeg-readable audio/video containers under an input directory:</p> <pre><code>data/raw_mp3/\n\u251c\u2500\u2500 speaker_a/\n\u2502   \u251c\u2500\u2500 clip_001.mp3\n\u2502   \u2514\u2500\u2500 clip_002.mp3\n\u2514\u2500\u2500 speaker_b/\n    \u2514\u2500\u2500 clip_001.mp3</code></pre> <p>Discovery is recursive and deterministic. It ignores files with unsupported extensions by default. Use <code>--extensions all</code> to pass every regular file to FFmpeg and record unsupported inputs as per-file failures.</p>"},{"location":"getting-started/#expected-output","title":"Expected Output","text":"<p>The conversion command mirrors the input directory structure under the output directory:</p> <pre><code>data/wav16k/\n\u251c\u2500\u2500 speaker_a/\n\u2502   \u251c\u2500\u2500 clip_001.wav\n\u2502   \u2514\u2500\u2500 clip_002.wav\n\u2514\u2500\u2500 speaker_b/\n    \u2514\u2500\u2500 clip_001.wav</code></pre> <p>If a manifest path is provided, each source file gets one JSONL record with a status:</p> Status Meaning <code>ok</code> Conversion succeeded and validation passed. <code>conversion_failed</code> FFmpeg could not decode or write the file. <code>validation_failed</code> Output exists but does not match the requested spec."},{"location":"getting-started/#basic-workflow","title":"Basic Workflow","text":"<ol> <li>Install Python dependencies and FFmpeg.</li> <li>Run conversion through either the CLI or Python API.</li> <li>Inspect the manifest for failed files.</li> <li>Optionally run chunking through either the CLI or Python API to create speech-only segments.</li> </ol> <p>CLI:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --manifest data/manifest.jsonl</code></pre> <p>Python:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, build_manifest, convert_batch, validate_output, write_manifest\n\nconfig = ConversionConfig()\nresults = convert_batch(Path(\"data/raw_mp3\"), Path(\"data/wav16k\"), config)\nvalidations = {\n    result.output: validate_output(result.output, config)\n    for result in results\n    if result.success and result.output is not None\n}\nrecords = build_manifest(results, validations)\nwrite_manifest(records, Path(\"data/manifest.jsonl\"))</code></pre> <p>The default conversion target is 16 kHz mono WAV. Override it only when the downstream model expects a different audio spec.</p> <p>CLI chunking:</p> <pre><code>audio-prep chunk \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/chunks \\\n    --min-duration-sec 5 \\\n    --max-duration-sec 20 \\\n    --manifest data/chunk_manifest.jsonl</code></pre> <p>Python chunking:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ChunkConfig, build_chunk_manifest, chunk_batch, write_manifest\n\nconfig = ChunkConfig(min_duration_sec=5, max_duration_sec=20)\nresults = chunk_batch(Path(\"data/raw_mp3\"), Path(\"data/chunks\"), config)\nrecords = build_chunk_manifest(results)\nwrite_manifest(records, Path(\"data/chunk_manifest.jsonl\"))</code></pre>"},{"location":"getting-started/#resume-behavior","title":"Resume Behavior","text":"<p>With <code>--overwrite</code> off, conversion skips an existing output only when it passes the same validation checks as a newly converted file. Empty, corrupt, too-short, or wrong-spec outputs are replaced automatically.</p> <p>Use <code>--overwrite</code> when you want to force regeneration even for valid existing outputs.</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10 or newer</li> <li>FFmpeg and FFprobe available on <code>PATH</code></li> <li><code>libsndfile</code>, usually installed automatically with <code>soundfile</code> wheels on   common platforms</li> </ul>"},{"location":"installation/#install-ffmpeg","title":"Install FFmpeg","text":"<p>macOS:</p> <pre><code>brew install ffmpeg</code></pre> <p>Ubuntu or Debian:</p> <pre><code>sudo apt-get update\nsudo apt-get install ffmpeg</code></pre> <p>Check that both binaries are available:</p> <pre><code>ffmpeg -version\nffprobe -version</code></pre>"},{"location":"installation/#install-the-package","title":"Install The Package","text":"<p>For local development:</p> <pre><code>make install</code></pre> <p>This runs:</p> <pre><code>pip install -e \".[dev]\"\npre-commit install</code></pre> <p>For installation from PyPI:</p> <pre><code>pip install audio-prep-pipeline</code></pre> <p>Install directly from GitHub:</p> <pre><code>pip install \"audio-prep-pipeline @ git+https://github.com/nattkorat/audio-prep-pipeline.git\"</code></pre> <p>For editable local usage without developer tools:</p> <pre><code>pip install -e .</code></pre> <p>For documentation work:</p> <pre><code>pip install -e \".[docs]\"</code></pre>"},{"location":"installation/#chunking-dependencies","title":"Chunking Dependencies","text":"<p>The standard package install includes <code>torch</code>, <code>silero-vad</code>, and <code>tqdm</code>, so both <code>audio-prep convert</code> and <code>audio-prep chunk</code> are ready after:</p> <pre><code>pip install audio-prep-pipeline</code></pre> <p>The first chunking run loads Silero from the installed <code>silero-vad</code> package. If Silero cannot load in an offline environment, pass <code>--allow-energy-fallback</code> to use the lower-quality offline energy detector.</p>"},{"location":"quickstart/","title":"Quick Start","text":""},{"location":"quickstart/#convert-source-audio-to-16-khz-mono-wav","title":"Convert Source Audio To 16 kHz Mono WAV","text":"<p>CLI:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --workers 4 \\\n    --manifest data/manifest.jsonl</code></pre> <p>Python:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, build_manifest, convert_batch, validate_output, write_manifest\n\nconfig = ConversionConfig(num_workers=4)\nresults = convert_batch(Path(\"data/raw_mp3\"), Path(\"data/wav16k\"), config)\nvalidations = {\n    result.output: validate_output(result.output, config)\n    for result in results\n    if result.success and result.output is not None\n}\nrecords = build_manifest(results, validations)\nwrite_manifest(records, Path(\"data/manifest.jsonl\"))</code></pre> <p>Expected result:</p> <ul> <li>Converted files under <code>data/wav16k</code></li> <li>Matching subdirectories from <code>data/raw_mp3</code></li> <li>A JSONL manifest at <code>data/manifest.jsonl</code></li> <li>Exit code <code>0</code> if every file converted and validated</li> <li>Exit code <code>1</code> if any file failed conversion or validation</li> </ul>"},{"location":"quickstart/#convert-to-flac","title":"Convert To FLAC","text":"<p>CLI:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/flac16k \\\n    --format flac \\\n    --manifest data/flac_manifest.jsonl</code></pre> <p>Python:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, build_manifest, convert_batch, validate_output, write_manifest\n\nconfig = ConversionConfig(output_format=\"flac\", num_workers=4)\nresults = convert_batch(Path(\"data/raw_mp3\"), Path(\"data/flac16k\"), config)\nvalidations = {\n    result.output: validate_output(result.output, config)\n    for result in results\n    if result.success and result.output is not None\n}\nrecords = build_manifest(results, validations)\nwrite_manifest(records, Path(\"data/flac_manifest.jsonl\"))</code></pre>"},{"location":"quickstart/#force-rebuild-existing-outputs","title":"Force Rebuild Existing Outputs","text":"<p>CLI:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --overwrite</code></pre> <p>Python:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, convert_batch\n\nconfig = ConversionConfig(overwrite=True)\nresults = convert_batch(Path(\"data/raw_mp3\"), Path(\"data/wav16k\"), config)</code></pre> <p>Without <code>--overwrite</code>, valid existing outputs are reused and invalid existing outputs are replaced.</p>"},{"location":"quickstart/#chunk-speech","title":"Chunk Speech","text":"<p>CLI:</p> <pre><code>audio-prep chunk \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/chunks \\\n    --min-duration-sec 5 \\\n    --max-duration-sec 20 \\\n    --workers 4 \\\n    --manifest data/chunk_manifest.jsonl</code></pre> <p>Python:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ChunkConfig, build_chunk_manifest, chunk_batch, write_manifest\n\nconfig = ChunkConfig(min_duration_sec=5, max_duration_sec=20, num_workers=4)\nresults = chunk_batch(Path(\"data/raw_mp3\"), Path(\"data/chunks\"), config)\nrecords = build_chunk_manifest(results)\nwrite_manifest(records, Path(\"data/chunk_manifest.jsonl\"))</code></pre> <p>Use <code>--allow-energy-fallback</code> only when Silero VAD is not available and an approximate offline detector is acceptable.</p>"},{"location":"api/chunker/","title":"Chunker API","text":""},{"location":"api/chunker/#chunkconfig","title":"<code>ChunkConfig</code>","text":"<p>Configuration dataclass for VAD chunking.</p> Field Type Default <code>min_duration_sec</code> <code>float</code> <code>5.0</code> <code>max_duration_sec</code> <code>float</code> <code>20.0</code> <code>output_format</code> <code>str</code> <code>wav</code> <code>sample_rate</code> <code>Optional[int]</code> <code>None</code> <code>num_workers</code> <code>int</code> <code>1</code> <code>overwrite</code> <code>bool</code> <code>False</code> <code>allow_energy_fallback</code> <code>bool</code> <code>False</code>"},{"location":"api/chunker/#chunkresult","title":"<code>ChunkResult</code>","text":"Field Type <code>source</code> <code>Path</code> <code>chunks</code> <code>list[Path]</code> <code>success</code> <code>bool</code> <code>error</code> <code>Optional[str]</code>"},{"location":"api/chunker/#chunk_filesource-output_dir-confignone-detectnone","title":"<code>chunk_file(source, output_dir, config=None, detect=None)</code>","text":"<p>Chunks one source file. The optional <code>detect</code> argument is primarily useful for tests or custom VAD implementations.</p>"},{"location":"api/chunker/#chunk_batchinput_dir-output_dir-confignone-source_filesnone","title":"<code>chunk_batch(input_dir, output_dir, config=None, source_files=None)</code>","text":"<p>Discovers supported source audio files under <code>input_dir</code> unless <code>source_files</code> is supplied, then chunks each file.</p> <p>Example:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ChunkConfig, build_chunk_manifest, chunk_batch, write_manifest\n\nconfig = ChunkConfig(min_duration_sec=5, max_duration_sec=20, sample_rate=16_000)\nresults = chunk_batch(Path(\"data/raw_mp3\"), Path(\"data/chunks\"), config)\nrecords = build_chunk_manifest(results)\nwrite_manifest(records, Path(\"data/chunk_manifest.jsonl\"))</code></pre>"},{"location":"api/config/","title":"Config API","text":""},{"location":"api/config/#conversionconfig","title":"<code>ConversionConfig</code>","text":"<pre><code>from audio_prep import ConversionConfig</code></pre> <p>Immutable dataclass for conversion and validation settings.</p> <p>Fields:</p> Field Type Default <code>output_format</code> <code>str</code> <code>wav</code> <code>sample_rate</code> <code>int</code> <code>16000</code> <code>channels</code> <code>int</code> <code>1</code> <code>num_workers</code> <code>int</code> <code>4</code> <code>min_duration_sec</code> <code>float</code> <code>0.5</code> <code>overwrite</code> <code>bool</code> <code>False</code> <code>normalize_loudness</code> <code>bool</code> <code>False</code> <p>Supported formats are <code>wav</code> and <code>flac</code>. Invalid numeric values raise <code>ValueError</code>.</p>"},{"location":"api/converter/","title":"Converter API","text":""},{"location":"api/converter/#supported_source_extensions","title":"<code>SUPPORTED_SOURCE_EXTENSIONS</code>","text":"<p>Tuple of common FFmpeg-readable source extensions discovered by default.</p>"},{"location":"api/converter/#default_source_extensions","title":"<code>DEFAULT_SOURCE_EXTENSIONS</code>","text":"<p>Alias for the default source extension tuple used by CLI discovery.</p>"},{"location":"api/converter/#conversionresult","title":"<code>ConversionResult</code>","text":"<p>Result dataclass returned by conversion functions.</p> Field Type <code>source</code> <code>Path</code> <code>output</code> <code>Optional[Path]</code> <code>success</code> <code>bool</code> <code>error</code> <code>Optional[str]</code>"},{"location":"api/converter/#find_audio_filesinput_dir-extensionsdefault_source_extensions","title":"<code>find_audio_files(input_dir, extensions=DEFAULT_SOURCE_EXTENSIONS)</code>","text":"<p>Recursively finds source audio files under <code>input_dir</code>.</p> <p>Pass <code>extensions=None</code> to return every regular file and let FFmpeg decide whether each input is decodable.</p>"},{"location":"api/converter/#convert_filesource-dest-config","title":"<code>convert_file(source, dest, config)</code>","text":"<p>Converts one source file to one destination path.</p> <p>Returns <code>ConversionResult</code> instead of raising for file-level failures such as:</p> <ul> <li>missing source file</li> <li>missing FFmpeg binary</li> <li>FFmpeg conversion failure</li> <li>invalid existing output that cannot be replaced</li> </ul>"},{"location":"api/converter/#convert_batchinput_dir-output_dir-confignone-source_filesnone","title":"<code>convert_batch(input_dir, output_dir, config=None, source_files=None)</code>","text":"<p>Converts all discovered or supplied source files and mirrors the input directory structure under <code>output_dir</code>.</p> <p>Example:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, build_manifest, convert_batch, validate_output, write_manifest\n\nconfig = ConversionConfig(num_workers=4)\nresults = convert_batch(\n    Path(\"data/raw_mp3\"),\n    Path(\"data/wav16k\"),\n    config,\n)\nvalidations = {\n    result.output: validate_output(result.output, config)\n    for result in results\n    if result.success and result.output is not None\n}\nrecords = build_manifest(results, validations)\nwrite_manifest(records, Path(\"data/manifest.jsonl\"))</code></pre>"},{"location":"api/exceptions/","title":"Exceptions API","text":""},{"location":"api/exceptions/#audiopreperror","title":"<code>AudioPrepError</code>","text":"<p>Base exception for package-specific errors.</p>"},{"location":"api/exceptions/#conversionerror","title":"<code>ConversionError</code>","text":"<p>Represents a failed FFmpeg conversion. It includes:</p> <ul> <li>source path</li> <li>FFmpeg return code</li> <li>stderr text</li> </ul> <p><code>convert_file</code> catches this internally and returns a failed <code>ConversionResult</code>.</p>"},{"location":"api/exceptions/#probeerror","title":"<code>ProbeError</code>","text":"<p>Raised by <code>probe_duration</code> when FFprobe fails or returns an invalid duration.</p>"},{"location":"api/exceptions/#chunkingerror","title":"<code>ChunkingError</code>","text":"<p>Raised internally when VAD chunking cannot produce usable chunks. <code>chunk_file</code> catches it and returns a failed <code>ChunkResult</code>.</p>"},{"location":"api/manifest/","title":"Manifest API","text":""},{"location":"api/manifest/#manifestrecord","title":"<code>ManifestRecord</code>","text":"<p>Conversion manifest row.</p> Field Type <code>source_path</code> <code>str</code> <code>output_path</code> <code>Optional[str]</code> <code>status</code> <code>str</code> <code>duration_sec</code> <code>Optional[float]</code> <code>sample_rate</code> <code>Optional[int]</code> <code>channels</code> <code>Optional[int]</code> <code>error</code> <code>Optional[str]</code>"},{"location":"api/manifest/#chunkmanifestrecord","title":"<code>ChunkManifestRecord</code>","text":"<p>Chunk manifest row.</p> Field Type <code>source_path</code> <code>str</code> <code>status</code> <code>str</code> <code>num_chunks</code> <code>int</code> <code>chunk_paths</code> <code>list[str]</code> <code>error</code> <code>Optional[str]</code>"},{"location":"api/manifest/#build_manifestconversion_results-validation_results","title":"<code>build_manifest(conversion_results, validation_results)</code>","text":"<p>Builds conversion manifest records from conversion and validation results.</p>"},{"location":"api/manifest/#build_chunk_manifestchunk_results","title":"<code>build_chunk_manifest(chunk_results)</code>","text":"<p>Builds chunk manifest records from chunking results.</p>"},{"location":"api/manifest/#write_manifestrecords-manifest_path","title":"<code>write_manifest(records, manifest_path)</code>","text":"<p>Writes any manifest record list to JSONL and creates parent directories.</p>"},{"location":"api/validator/","title":"Validator API","text":""},{"location":"api/validator/#validationresult","title":"<code>ValidationResult</code>","text":"Field Type <code>path</code> <code>Path</code> <code>valid</code> <code>bool</code> <code>duration_sec</code> <code>Optional[float]</code> <code>sample_rate</code> <code>Optional[int]</code> <code>channels</code> <code>Optional[int]</code> <code>reason</code> <code>Optional[str]</code>"},{"location":"api/validator/#validate_outputpath-config","title":"<code>validate_output(path, config)</code>","text":"<p>Validates a converted output file against <code>ConversionConfig</code>.</p> <p>Example:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, validate_output\n\nresult = validate_output(Path(\"data/wav16k/a.wav\"), ConversionConfig())</code></pre>"},{"location":"api/validator/#probe_durationpath","title":"<code>probe_duration(path)</code>","text":"<p>Uses FFprobe to return duration in seconds. Raises <code>ProbeError</code> when FFprobe fails or returns an unparseable duration.</p>"},{"location":"development/contributing/","title":"Contributing","text":""},{"location":"development/contributing/#setup","title":"Setup","text":"<pre><code>make install</code></pre> <p>This installs the package in editable mode with development dependencies and sets up pre-commit hooks.</p>"},{"location":"development/contributing/#before-opening-a-pr","title":"Before Opening A PR","text":"<p>Run:</p> <pre><code>make check</code></pre> <p>This runs:</p> <ul> <li><code>ruff check .</code></li> <li><code>mypy src</code></li> <li><code>pytest</code></li> </ul>"},{"location":"development/contributing/#coding-guidelines","title":"Coding Guidelines","text":"<ul> <li>Keep pipeline behavior per-file and resumable.</li> <li>Return structured results for recoverable file-level failures.</li> <li>Preserve deterministic ordering for batch results.</li> <li>Add focused tests for new behavior.</li> <li>Keep conversion-only paths free of chunking-only dependency imports.</li> </ul>"},{"location":"development/contributing/#branching","title":"Branching","text":"<p>The main branch is protected. Use a feature branch for changes.</p>"},{"location":"development/roadmap/","title":"Roadmap","text":"<p>Likely next improvements:</p>"},{"location":"development/roadmap/#streaming-manifest-writes","title":"Streaming Manifest Writes","text":"<p>Very large corpora currently build result lists before manifest writing. Streaming records during conversion would reduce memory use and make progress visible earlier.</p>"},{"location":"development/roadmap/#richer-progress-reporting","title":"Richer Progress Reporting","text":"<p>Chunking has progress bars. Conversion could add similar reporting and summary counts for large runs.</p>"},{"location":"development/roadmap/#dataset-splits","title":"Dataset Splits","text":"<p>Future manifest tooling could create train, validation, and test splits after validation.</p>"},{"location":"development/testing/","title":"Testing","text":"<p>The test suite uses generated audio fixtures instead of checked-in binary audio. Synthetic MP3 and WAV files are created with FFmpeg during tests.</p>"},{"location":"development/testing/#run-tests","title":"Run Tests","text":"<pre><code>make test</code></pre> <p>or:</p> <pre><code>PYTHONPATH=src pytest</code></pre>"},{"location":"development/testing/#run-focused-tests","title":"Run Focused Tests","text":"<pre><code>PYTHONPATH=src pytest tests/test_converter.py\nPYTHONPATH=src pytest tests/test_chunker.py</code></pre>"},{"location":"development/testing/#lint-and-typecheck","title":"Lint And Typecheck","text":"<pre><code>make lint\nmake typecheck</code></pre>"},{"location":"development/testing/#full-local-check","title":"Full Local Check","text":"<pre><code>make check</code></pre>"},{"location":"development/testing/#build-documentation","title":"Build Documentation","text":"<pre><code>make docs</code></pre> <p>For live preview:</p> <pre><code>make docs-serve</code></pre>"},{"location":"development/testing/#notes","title":"Notes","text":"<p>Some tests exercise <code>ProcessPoolExecutor</code>. In restricted environments, these may need normal OS semaphore access.</p>"},{"location":"pipeline/chunking/","title":"Chunking","text":"<p>Chunking splits source audio into speech-only chunks. It is implemented in <code>audio_prep.chunker</code> and exposed through <code>audio-prep chunk</code>.</p>"},{"location":"pipeline/chunking/#detector","title":"Detector","text":"<p>The preferred detector is Silero VAD. The project tries to load Silero from the installed <code>silero-vad</code> package first, then from <code>torch.hub</code>.</p> <p>If Silero is unavailable and <code>allow_energy_fallback=True</code>, chunking uses a simple energy-based detector. This fallback is useful for offline testing but is lower quality than Silero.</p>"},{"location":"pipeline/chunking/#decode-and-resample","title":"Decode And Resample","text":"<p>Compressed/container sources are decoded through FFmpeg. WAV and FLAC files may be read directly with <code>soundfile</code> when no resampling is needed.</p> <p>If a sample rate is configured, audio is resampled before VAD and the written chunks use that rate.</p>"},{"location":"pipeline/chunking/#duration-rules","title":"Duration Rules","text":"<ul> <li>Speech shorter than <code>min_duration_sec</code> is dropped.</li> <li>Speech longer than <code>max_duration_sec</code> is split into windows.</li> <li>A source file that produces no chunks returns a failed <code>ChunkResult</code>.</li> </ul>"},{"location":"pipeline/chunking/#existing-chunks","title":"Existing Chunks","text":"<p>When <code>overwrite=False</code>, existing chunks are reused only if they are readable and match the expected sample rate. Corrupt existing chunks are rewritten.</p> <p>If two source files in the same directory share a stem, such as <code>clip.mp3</code> and <code>clip.wav</code>, chunk outputs are written into disambiguated subdirectories such as <code>clip_mp3/</code> and <code>clip_wav/</code>.</p>"},{"location":"pipeline/chunking/#cli-example","title":"CLI Example","text":"<pre><code>audio-prep chunk \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/chunks \\\n    --format flac \\\n    --sample-rate 16000 \\\n    --min-duration-sec 5 \\\n    --max-duration-sec 20 \\\n    --workers 4 \\\n    --manifest data/chunk_manifest.jsonl</code></pre>"},{"location":"pipeline/chunking/#python-example","title":"Python Example","text":"<pre><code>from pathlib import Path\n\nfrom audio_prep import ChunkConfig, build_chunk_manifest, chunk_batch, write_manifest\n\nconfig = ChunkConfig(\n    min_duration_sec=5,\n    max_duration_sec=20,\n    output_format=\"flac\",\n    sample_rate=16_000,\n    num_workers=4,\n)\n\nresults = chunk_batch(\n    Path(\"data/raw_mp3\"),\n    Path(\"data/chunks\"),\n    config,\n)\nrecords = build_chunk_manifest(results)\nwrite_manifest(records, Path(\"data/chunk_manifest.jsonl\"))</code></pre>"},{"location":"pipeline/conversion/","title":"Conversion","text":"<p>Conversion is handled by FFmpeg through <code>convert_file</code> and <code>convert_batch</code>.</p>"},{"location":"pipeline/conversion/#cli-example","title":"CLI Example","text":"<pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --format wav \\\n    --sample-rate 16000 \\\n    --channels 1 \\\n    --workers 4 \\\n    --manifest data/manifest.jsonl</code></pre>"},{"location":"pipeline/conversion/#python-example","title":"Python Example","text":"<pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, build_manifest, convert_batch, validate_output, write_manifest\n\nconfig = ConversionConfig(\n    output_format=\"wav\",\n    sample_rate=16_000,\n    channels=1,\n    num_workers=4,\n)\n\nresults = convert_batch(Path(\"data/raw_mp3\"), Path(\"data/wav16k\"), config)\nvalidations = {\n    result.output: validate_output(result.output, config)\n    for result in results\n    if result.success and result.output is not None\n}\nrecords = build_manifest(results, validations)\nwrite_manifest(records, Path(\"data/manifest.jsonl\"))</code></pre>"},{"location":"pipeline/conversion/#output-path-rules","title":"Output Path Rules","text":"<p>For batch conversion, the output path is derived from the source path relative to <code>input_dir</code>:</p> <pre><code>data/raw_mp3/speaker_a/clip_001.mp3</code></pre> <p>converted to WAV under <code>data/wav16k</code> becomes:</p> <pre><code>data/wav16k/speaker_a/clip_001.wav</code></pre> <p>If two source files in the same directory would produce the same output path, the original source extension is added to the output stem. For example, <code>clip.mp3</code> and <code>clip.wav</code> become <code>clip_mp3.wav</code> and <code>clip_wav.wav</code>.</p>"},{"location":"pipeline/conversion/#ffmpeg-settings","title":"FFmpeg Settings","text":"<p>The converter always sets:</p> <ul> <li><code>-ar</code> to the configured sample rate</li> <li><code>-ac</code> to the configured channel count</li> <li>WAV output as signed 16-bit PCM</li> <li>FLAC output with the FLAC muxer</li> </ul> <p>When <code>normalize_loudness=True</code>, FFmpeg also receives:</p> <pre><code>loudnorm=I=-23:LRA=7:TP=-2</code></pre>"},{"location":"pipeline/conversion/#resume-behavior","title":"Resume Behavior","text":"<p>When <code>overwrite=False</code>, an existing output is reused only if it passes validation for the current target spec. Invalid existing outputs are removed and converted again.</p> <p>If an invalid existing output cannot be removed, the file is reported as a failed <code>ConversionResult</code> instead of raising out of the batch.</p>"},{"location":"pipeline/conversion/#batch-behavior","title":"Batch Behavior","text":"<p><code>convert_batch</code> uses a process pool when <code>num_workers &gt; 1</code>. Results are sorted back into discovery order before being returned.</p> <p>Bad source files do not stop the run. They return <code>ConversionResult</code> objects with <code>success=False</code> and an error message.</p>"},{"location":"pipeline/discovery/","title":"Discovery","text":"<p>Discovery is handled by <code>find_audio_files</code>.</p>"},{"location":"pipeline/discovery/#behavior","title":"Behavior","text":"<ul> <li>Recursively walks the input directory.</li> <li>Selects files by extension.</li> <li>Matches extensions case-insensitively.</li> <li>Returns sorted paths for deterministic runs.</li> <li>Raises <code>FileNotFoundError</code> when the input directory does not exist.</li> </ul> <p>The default extension set includes common FFmpeg-readable audio/video containers such as:</p> <pre><code>(\".mp3\", \".wav\", \".flac\", \".m4a\", \".ogg\", \".opus\", \".webm\", \".mp4\", ...)</code></pre>"},{"location":"pipeline/discovery/#python-example","title":"Python Example","text":"<pre><code>from pathlib import Path\n\nfrom audio_prep import find_audio_files\n\nfiles = find_audio_files(Path(\"data/raw_mp3\"))</code></pre>"},{"location":"pipeline/discovery/#custom-extensions","title":"Custom Extensions","text":"<p>The function accepts a custom extension tuple:</p> <pre><code>files = find_audio_files(\n    Path(\"data/audio\"),\n    extensions=(\".mp3\", \".m4a\", \".ogg\"),\n)</code></pre> <p>Pass <code>extensions=None</code> to return every regular file and let FFmpeg decide what can be decoded:</p> <pre><code>files = find_audio_files(Path(\"data/mixed\"), extensions=None)</code></pre>"},{"location":"pipeline/discovery/#cli-example","title":"CLI Example","text":"<p>The CLI exposes the same behavior with <code>--extensions</code>:</p> <pre><code>audio-prep convert \\\n    --input-dir data/audio \\\n    --output-dir data/wav16k \\\n    --extensions mp3,m4a,ogg\n\naudio-prep chunk \\\n    --input-dir data/mixed \\\n    --output-dir data/chunks \\\n    --extensions all</code></pre> <p>Use <code>--extensions all</code> to scan every regular file.</p>"},{"location":"pipeline/manifest/","title":"Manifest","text":"<p>Manifests are JSONL files. Each line is one JSON object.</p>"},{"location":"pipeline/manifest/#conversion-manifest","title":"Conversion Manifest","text":"<p><code>build_manifest</code> combines conversion results and validation results.</p> <p>Fields:</p> Field Type Meaning <code>source_path</code> string Original source file path. <code>output_path</code> string or null Converted output path, when available. <code>status</code> string <code>ok</code>, <code>conversion_failed</code>, or <code>validation_failed</code>. <code>duration_sec</code> number or null Validated duration. <code>sample_rate</code> number or null Validated sample rate. <code>channels</code> number or null Validated channel count. <code>error</code> string or null Error or validation reason. <p>Example:</p> <pre><code>{\"source_path\":\"data/raw_mp3/a.mp3\",\"output_path\":\"data/wav16k/a.wav\",\"status\":\"ok\",\"duration_sec\":1.0,\"sample_rate\":16000,\"channels\":1,\"error\":null}</code></pre>"},{"location":"pipeline/manifest/#chunk-manifest","title":"Chunk Manifest","text":"<p><code>build_chunk_manifest</code> records one row per source file.</p> <p>Fields:</p> Field Type Meaning <code>source_path</code> string Original source file path. <code>status</code> string <code>ok</code> or <code>chunking_failed</code>. <code>num_chunks</code> number Number of chunks written. <code>chunk_paths</code> list[string] Written chunk paths. <code>error</code> string or null Chunking error, when any."},{"location":"pipeline/manifest/#writing-manifests","title":"Writing Manifests","text":"<p>CLI:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --manifest data/manifest.jsonl\n\naudio-prep chunk \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/chunks \\\n    --manifest data/chunk_manifest.jsonl</code></pre> <p>Python:</p> <p>Use <code>write_manifest</code> for both conversion and chunk records:</p> <pre><code>from audio_prep.manifest import write_manifest\n\nwrite_manifest(records, \"data/manifest.jsonl\")</code></pre> <p>Parent directories are created automatically.</p>"},{"location":"pipeline/overview/","title":"Pipeline Overview","text":"<p><code>audio-prep</code> has two independent pipelines.</p>"},{"location":"pipeline/overview/#conversion-pipeline","title":"Conversion Pipeline","text":"<pre><code>Raw source audio\n  -&gt; discovery\n  -&gt; FFmpeg conversion\n  -&gt; validation\n  -&gt; JSONL manifest\n  -&gt; training-ready WAV or FLAC files</code></pre> <p>Conversion failures do not abort the whole run. Each source file receives a result, and failed files can be inspected through the manifest.</p>"},{"location":"pipeline/overview/#chunking-pipeline","title":"Chunking Pipeline","text":"<pre><code>Raw source audio\n  -&gt; discovery\n  -&gt; FFmpeg decode and optional resample\n  -&gt; VAD speech detection\n  -&gt; chunk writing\n  -&gt; JSONL chunk manifest</code></pre> <p>Chunking is separate from conversion. The <code>chunk</code> command scans source audio files directly and writes chunks without first running <code>convert</code>.</p>"},{"location":"pipeline/overview/#deterministic-output","title":"Deterministic Output","text":"<p>Discovery returns sorted file paths so sequential and parallel runs produce results in the same order. Output paths mirror source subdirectories under the chosen output directory.</p>"},{"location":"pipeline/overview/#cli-or-python","title":"CLI Or Python","text":"<p>Run the pipelines directly from the command line:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --manifest data/manifest.jsonl\n\naudio-prep chunk \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/chunks \\\n    --manifest data/chunk_manifest.jsonl</code></pre> <p>Or call the same pipeline functions from Python:</p> <pre><code>from pathlib import Path\n\nfrom audio_prep import ChunkConfig, ConversionConfig, chunk_batch, convert_batch\n\nconverted = convert_batch(\n    Path(\"data/raw_mp3\"),\n    Path(\"data/wav16k\"),\n    ConversionConfig(),\n)\n\nchunks = chunk_batch(\n    Path(\"data/raw_mp3\"),\n    Path(\"data/chunks\"),\n    ChunkConfig(),\n)</code></pre>"},{"location":"pipeline/validation/","title":"Validation","text":"<p>Validation checks converted output before it is accepted into a manifest.</p>"},{"location":"pipeline/validation/#checks","title":"Checks","text":"<p><code>validate_output</code> checks, in order:</p> <ol> <li>The path exists and is a file.</li> <li>The file is not empty.</li> <li><code>soundfile</code> can read the audio header.</li> <li>The sample rate matches <code>ConversionConfig.sample_rate</code>.</li> <li>The channel count matches <code>ConversionConfig.channels</code>.</li> <li>Duration is at least <code>ConversionConfig.min_duration_sec</code>.</li> </ol> <p>Failures return a <code>ValidationResult</code> with <code>valid=False</code> and a human-readable reason.</p>"},{"location":"pipeline/validation/#duration-probing","title":"Duration Probing","text":"<p><code>probe_duration</code> uses FFprobe and can inspect formats that <code>soundfile</code> may not read directly, including MP3.</p>"},{"location":"pipeline/validation/#cli-example","title":"CLI Example","text":"<p>Validation runs automatically after every successful conversion:</p> <pre><code>audio-prep convert \\\n    --input-dir data/raw_mp3 \\\n    --output-dir data/wav16k \\\n    --sample-rate 16000 \\\n    --channels 1 \\\n    --min-duration-sec 0.5 \\\n    --manifest data/manifest.jsonl</code></pre>"},{"location":"pipeline/validation/#python-example","title":"Python Example","text":"<pre><code>from pathlib import Path\n\nfrom audio_prep import ConversionConfig, validate_output\n\nconfig = ConversionConfig(sample_rate=16_000, channels=1)\nresult = validate_output(Path(\"data/wav16k/clip.wav\"), config)\n\nif not result.valid:\n    print(result.reason)</code></pre>"}]}